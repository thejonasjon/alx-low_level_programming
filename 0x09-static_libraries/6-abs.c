#include"main.h"
#include <stdlib.h>

/**
 *_abs - fuction to return absolute value
 *@n: argument to return convert value
 *	into absolute
 *
 *Return: Always n absolute
 */

int _abs(int n)
{
	/*abs function to compute absolute value*/
	return (abs(n));
}
