#include"main.h"
#include<stdio.h>

/**
 * _puts - function that prints a string,
 * followed by a new line, to stdout
 *
 * @str: argument
 *
 * Return: Always 0 (success)
 */

void _puts(char *str)
{
	puts(str);
}
