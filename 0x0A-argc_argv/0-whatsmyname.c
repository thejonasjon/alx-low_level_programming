#include"main.h"
#include <stdio.h>

/**
 * main - a program to prints the program name, followed by a new line.
 * @argc:first argument
 * @argv: second argument
 *
 * Return: Always 0 (success)
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
