#include"main.h"
#include<string.h>

/**
 * _strlen - function that returns the length of a string
 *
 * @s: argument
 *
 * Return: length of a sting
 */

int _strlen(char *s)
{
	return (strlen(s));
}
